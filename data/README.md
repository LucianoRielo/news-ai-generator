# Data

This directory is intentionally split into:

- `raw/`: downloaded source files normalized into CSVs.
- `processed/`: JSONL splits ready for language-model fine-tuning.

Large datasets and generated artifacts should stay out of git.
